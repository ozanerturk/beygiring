# .github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run checks
        run: echo "CI passed"
